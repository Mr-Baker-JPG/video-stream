generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model links {
  email      String
  key        String    @id
  /// The value of this field is generated by the database as: `'2021-01-21 22:21:16.915279'::timestamp without time zone`.
  created_at DateTime? @default(dbgenerated())
}
